MODULE: frontend/src/hooks
FILES: useThemeFilters.ts
RESPONSIBILITY: 自定义 React Hooks，提供状态管理和副作用逻辑复用
DEPENDENCIES:
- uses: react: useState, useEffect, useCallback
- uses: react-router-dom: useSearchParams URL 参数同步
- imports: @/types/theme: ThemeListParams 类型
KEY_FILES:
- useThemeFilters.ts: URL 同步的题材筛选状态 Hook，含 300ms 搜索防抖，120行
INTERFACES:
- useThemeFilters(): 返回 filters, searchInput, setSearchInput, updateFilter, setPage, setSort, clearFilters, activeFilterCount
TRAPS:
- [LOW] useThemeFilters 的 useEffect 依赖 searchInput 和 setSearchParams，初始挂载会触发一次防抖更新（无害，因 replace:true 且值相同）

DECISIONS:
- 搜索防抖使用 useEffect + setTimeout 实现（300ms），而非 useDeferredValue，因需精确控制延迟时间
- 使用 useSearchParams 的 replace:true 避免在浏览器历史中留下中间状态