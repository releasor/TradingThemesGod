MODULE: docker
FILES: docker-compose.yml, Makefile, .env.example, backend/Dockerfile, backend/entrypoint.sh, backend/.dockerignore, frontend/Dockerfile, frontend/nginx.conf
RESPONSIBILITY: Docker 部署配置，提供容器化部署和服务编排
KEY_FILES:
- docker-compose.yml: 服务编排配置，定义 db、backend、frontend 三个服务
- backend/Dockerfile: 后端多阶段构建，Python 3.12-slim 基础镜像
- frontend/Dockerfile: 前端多阶段构建，Node 20 构建 + Nginx 服务
- frontend/nginx.conf: Nginx 配置，SPA 服务和 API 代理
- backend/entrypoint.sh: 启动脚本，运行 Alembic 迁移后启动 uvicorn
- Makefile: 常用 Docker 操作命令
DEPENDENCIES:
- uses: docker: 容器运行时
- uses: docker-compose: 服务编排
- uses: nginx: 前端反向代理和静态文件服务
- uses: postgres:16: PostgreSQL 数据库
INTERFACES:
- docker-compose.yml: 定义三个服务 (db, backend, frontend) 及其依赖关系
- backend/Dockerfile: 多阶段构建，builder 阶段安装依赖，runtime 阶段运行应用
- frontend/Dockerfile: 多阶段构建，builder 阶段构建 React 应用，nginx 阶段提供服务
- Makefile: 提供 up, down, logs, build, migrate, clean 等命令
TRAPS:
- [HIGH] 后端 Dockerfile 使用 COPY . . 会复制整个目录 | FIX: 使用 .dockerignore 排除不必要的文件
- [LOW] 前端 Dockerfile 需要 pnpm-lock.yaml 文件 | FIX: 确保 pnpm-lock.yaml 存在并提交到版本控制
- [MEDIUM] Docker Compose 使用 depends_on 和 healthcheck 确保服务顺序 | FIX: 数据库健康检查使用 pg_isready
- [LOW] Nginx 配置代理 /api/ 到后端服务 | FIX: 确保后端服务名称与 docker-compose.yml 中一致
DECISIONS:
- 使用多阶段构建减小最终镜像大小
- 后端入口脚本先运行 Alembic 迁移再启动 uvicorn
- PostgreSQL 16 使用命名卷 postgres_data 持久化数据
- 前端构建上下文设置为项目根目录以便访问 nginx 配置
