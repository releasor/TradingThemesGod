MODULE: backend/app/models
FILES: base.py, theme.py, stock.py, event.py, industry_chain.py, theme_stock.py, __init__.py
RESPONSIBILITY: SQLAlchemy 数据模型定义，包含 5 个核心业务表
DEPENDENCIES:
- uses: sqlalchemy: ORM 框架
- uses: sqlalchemy.dialects.postgresql: JSONB 类型
KEY_FILES:
- base.py: 基类和 TimestampMixin，约 30 行
- theme.py: 题材模型，含软删除，约 80 行
- stock.py: 股票模型，约 50 行
- event.py: 事件模型，约 45 行
- industry_chain.py: 产业链模型，约 50 行
- theme_stock.py: 多对多关联表，约 40 行
INTERFACES:
- Theme(Base, TimestampMixin): 题材表，支持软删除（soft_delete/restore/is_deleted）
- Stock(Base, TimestampMixin): 股票表
- Event(Base, TimestampMixin): 事件表，外键关联 Stock
- IndustryChain(Base, TimestampMixin): 产业链表，外键关联 Theme
- ThemeStock(Base, TimestampMixin): 多对多关联表，复合主键 (theme_id, stock_id)
TRAPS:
- [HIGH] Stock.events 不使用 cascade="all, delete-orphan" — 与 Event.stock_id 的 ondelete="SET NULL" 冲突 | FIX: 使用默认 cascade 行为
- [HIGH] Theme.soft_delete() 使用 datetime.now(timezone.utc) — 不要使用 func.now()，它是 SQL 表达式对象 | FIX: 使用 Python datetime
- [MEDIUM] IndustryChain.level 有 CHECK 约束 — 只允许 upstream/midstream/downstream | FIX: 违反约束会抛出 IntegrityError
- [MEDIUM] JSON 字段使用 JSONB 类型 — PostgreSQL 中性能更好，支持索引
- [LOW] updated_at 的 onupdate 是 ORM 级别 — 原生 SQL 或批量操作需要手动更新
