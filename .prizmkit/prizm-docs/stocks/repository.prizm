MODULE: stocks/repository
FILES: app/repositories/stock.py
RESPONSIBILITY: 股票和事件数据库查询操作
KEY_FILES:
- app/repositories/stock.py: StockRepository 和 EventRepository 类，约 190 行
DEPENDENCIES:
- uses: sqlalchemy: 异步查询构建
- uses: sqlalchemy.orm: selectinload 预加载关联数据
- imports: app.models.stock: Stock 模型
- imports: app.models.event: Event 模型
INTERFACES:
- StockRepository(session: AsyncSession): 股票仓储
- list_paginated(page, page_size, sort_by, order, industry, exchange) -> tuple[list[Stock], int]: 分页查询
- get_by_code(code) -> Stock | None: 详情查询（含 selectinload events）
- get_events_by_code(code, page, page_size) -> tuple[list[Event], int]: 股票事件分页查询
- EventRepository(session: AsyncSession): 事件仓储
- list_paginated(page, page_size, event_type, sort_by, order) -> tuple[list[Event], int]: 分页查询
TRAPS:
- [LOW] sort_by 使用白名单验证（STOCK_SORT_FIELDS, EVENT_SORT_FIELDS）— 不验证时回退到默认字段 | FIX: 白名单外的值静默回退到 Stock.code / Event.published_at
